<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tijianapplication">

    <application
        android:name=".PartTimeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/tj_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tj_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.TiJianApplication">
        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.TiJianApplication.NoActionBar"></activity>
        <activity android:name=".activity.AboutUsActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TestResultActivity" />
    </application>

</manifest>